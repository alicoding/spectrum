exports.config = {

esServer : function (){
return 
var options = {
    host: 'localhost', // default
    port: 9200,        // default
    protocol: 'http',  // default
    index: 'spectrum', // optional - if set, then the core methods don't require an index
    type: 'post'
};

}

};